# Secrets — never commit these
backend/.env
backend/.env.*
.env
.env.*

# Claude Code internals
.claude/

# Deployment scripts contain env vars — use env vars instead
# deploy.sh is kept tracked but must never contain real keys

# Build artifacts & data
backend/venv/
backend/chroma_db/
backend/__pycache__/
backend/**/__pycache__/
backend/**/*.pyc

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local

# OS
.DS_Store

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.build]
dependsOn = ["install"]
cmds = ["python preindex.py"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
